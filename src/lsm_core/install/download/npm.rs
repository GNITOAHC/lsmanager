use crate::lsm::process_stdout::process_stdout;
use std::collections::HashMap;
use std::process::Command;
use std::process::Stdio;

pub fn handle_npm(source: &str, bin: &Option<HashMap<String, String>>) -> String {
    println!("handle_npm");
    return String::new();
}
